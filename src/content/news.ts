/**
 * Обновления школы для главной страницы.
 *
 * Лента заполняется по одному материалу: школа передаёт заголовок,
 * текст и снимок — материал добавляется в этот список. Формулировки
 * не переписываются (ТЗ §15), снимки не заменяются стоковыми (§23).
 *
 * Текст материала может не прийти сразу: тогда выводится только
 * заголовок со снимком, без придуманного описания.
 */
export type NewsItem = {
  title: string;
  /** Описание материала, если его передали. */
  text?: string;
  /** Телефон из текста новости, если он там есть. */
  phone?: { display: string; tel: string };
  image?: { src: string; width: number; height: number; alt: string };
};

/** Куда ведёт ссылка на все материалы. */
export const newsHref = '/meropriyatiya';

export const news: readonly NewsItem[] = [
  {
    title: 'Посещение нашей школы Мэром города Москвы - Собяниным Сергеем Семёновичем',
    image: {
      src: '/images/news-sobyanin.webp',
      width: 1200,
      height: 878,
      alt: 'Мэр Москвы Сергей Собянин с учениками школы за работой над проектом',
    },
  },
  {
    title:
      'Посещение нашей школы Российским государственным и политическим деятелем - ' +
      'Вассерманом Анатолием Александровичем',
    image: {
      src: '/images/news-wasserman.webp',
      width: 1200,
      height: 900,
      alt: 'Анатолий Вассерман с учениками школы в холле',
    },
  },
];
