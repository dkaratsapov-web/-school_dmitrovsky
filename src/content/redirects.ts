import type { Redirect } from './types';

/**
 * 301-редиректы.
 *
 * Заполняется ТОЛЬКО если адрес страницы пришлось изменить по технической
 * необходимости (ТЗ §12). По умолчанию все URL сохраняются как есть,
 * поэтому список пуст.
 */
export const redirects: readonly Redirect[] = [];
