/**
 * Тексты блока «Контакты» на главной.
 *
 * Сняты со страницы /contacts действующего сайта и перенесены дословно
 * (ТЗ §7). Сами контакты — телефоны, адрес, почта, соцсети — берутся
 * из общего источника `site.ts` и здесь не дублируются: иначе при правке
 * одного номера второй остался бы старым.
 */

/** Заголовок формы на странице контактов, дословно. */
export const consultTitle = 'Получите консультацию';

/** Подпись под заголовком формы, дословно. */
export const consultText =
  'Подробно проконсультируем по поступлению и обучению, ответим на все ' +
  'интересующие вас вопросы';

/** Подпись на кнопке формы, дословно. */
export const consultAction = 'Получить консультацию';

/** Подписи к контактным строкам — формулировки страницы. */
export const contactLabels = {
  address: 'Адрес',
  phone: 'Телефон',
  email: 'Электронная почта',
  socials: 'Соцсети',
} as const;
